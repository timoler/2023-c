# 四问建模题：双人 Git 协作规范

仓库：https://github.com/timoler/2023-c

## 一、分工和命名

不固定谁负责第几问，任何一人都可以接手任意问题。每次开始前，在聊天中说明“我正在改哪一问、哪些文件”，避免同时覆盖。每问的 README 记录当前维护者和最近一次交接情况。

- `main`：保存已检查、已整合的四问答案。
- `q1-assignment`：第一问工作分支。
- `q2-standard-score`：第二问工作分支。
- `q3-model`：第三问工作分支。
- `q4-model`：第四问工作分支。

如果两人同时改同一问，各自从最新 `main` 建立 `q题号-任务-姓名缩写` 分支，例如 `q2-weights-cjz`、`q2-figures-lm`，分别提交 PR。姓名缩写由本人确定。串行接手可以沿用已有工作分支，但要先 pull，并确认上一位已 push。

各自向当前任务的工作分支 push，完成一个可检查的阶段后发起 Pull Request（简称 PR），由另一人检查并合并到 `main`。不需要每改一个文件就合并，也不必等整问完全结束才合并。下文“必须提交”指该问交付时完整具备；阶段提交可以不齐，但必须标为“进行中”，列明缺项。

分支隔离开发工作，目录整理最终成果。建议逐步统一为：

```text
README.md
协作规范.md
题目/
数据/
问题一/
    README.md
    requirements.txt
    q1_solve.py
    q1_plot.py
    docs/
        q1_solution.md
    results/
    figures/
问题二/
问题三/
问题四/
论文/
```

这是目标结构，当前仓库尚未按此搬迁。调整现有文件位置时，需要同步修改代码中的读取路径。

- 原始数据保留原文件名和内容，计算结果另存到对应问题的 `results/`。
- 图表存到对应问题的 `figures/`，遵循下文的统一编号和命名。
- 每问的 `README.md` 写明模型思路、输入数据、运行命令、结果及待解决问题。
- 不使用“最终版2”“最终最终版”等文件名，历史版本由 Git 保存。
- 论文 Word 文件指定一人整合，另一人提供文字和图表，避免同时修改同一个二进制文件。

## 二、每问必须提交什么

每问交付时必须包含以下内容，不可以只传图片、只传代码或只写结论：

1. **说明入口 `README.md`**：按下一节模板填写，让队友能独立运行并找到成果。
2. **完整解答 `docs/qN_solution.md`**：题目分析、假设、符号、模型公式、求解步骤、结果解读、验证和局限。`N` 替换为 `1`～`4`。供论文直接引用的文字也放在这里。额外的 Word/PDF 可以提交，但不能代替可直接阅读的 Markdown 解答。
3. **可运行代码 `qN_solve.py`**：从输入数据生成计算结果。绘图代码统一为 `qN_plot.py`；若求解入口同时负责绘图，可以不单列绘图脚本，但 README 必须写清楚。其他语言使用对应扩展名，并说明运行环境。
4. **依赖 `requirements.txt`**：列出实际运行需要的 Python 包及本次验证版本，README 写明 Python 版本。其他语言提供对应依赖清单；没有第三方依赖也要明确说明。
5. **计算结果 `results/`**：上传最终结果明细、评价指标和图表底层数据，优先采用 UTF-8 CSV；多工作表数据可以用 XLSX。不能只留下终端截图。
6. **图表 `figures/`**：每问至少提交一张与实际结论相关的图，并提交所有被解答正文引用的图。按实际内容选择分布、方案对比、误差、敏感性或流程图，不为凑数量制造无意义图片。

原始题目数据只在仓库保留一份，README 用相对路径指向它。新增外部输入必须同时提供可用文件或明确的获取方式、来源和预处理步骤；不能依赖本人电脑里未提交的文件。

### 文件命名统一规则

- 问题目录固定为 `问题一/`、`问题二/`、`问题三/`、`问题四/`。
- 除固定文件 `README.md`、`requirements.txt` 和原始附件外，新增成果文件以 `q1_`、`q2_`、`q3_`、`q4_` 开头。
- 后续描述用英文小写和下划线，不使用空格。例如 `q2_expert_weights.csv`。
- 同一内容更新时沿用原文件名，通过 Git 留历史；只有需要并列比较的不同方法或实验才加语义后缀，例如 `q2_scores_baseline.csv`、`q2_scores_weighted.csv`。
- 不使用 `test.png`、`新建文档.docx`、`结果最终版.xlsx` 等无法判断用途的名字。

### 图表命名和交付要求

图片统一命名为 `qN_figXX_内容.png`，其中 `XX` 为问内两位编号，从 `01` 开始。例如：

```text
问题二/figures/q2_fig01_expert_distribution.png
问题二/figures/q2_fig02_score_comparison.png
问题二/figures/q2_fig03_expert_weights.png
```

- 每张图必须提供 PNG；用于论文的统计图同时提供同名 PDF 或 SVG 矢量版，绘图库不支持时在 README 说明，提供足够清晰的 PNG。
- PNG 按目标排版尺寸以至少 300 dpi 导出，建议宽度不少于 1800 像素；文字必须可读，无乱码、裁切或图例遮挡。不能仅修改 DPI 元数据冒充高清。
- 图中必须有必要的坐标名称、单位、图例；各问尽量统一字体、配色和小数位。概念流程图等无坐标图不要求坐标轴。
- 同一张图的 PNG/PDF/SVG 使用相同主文件名。正文按“图 2-1”等问内编号引用，并链接到对应文件；论文整合阶段再统一全篇编号。
- 已在正文引用的图号尽量保持稳定，新图追加编号；若调整编号，必须同时更新正文、README 和代码输出名。
- 统计图必须能从已提交的结果数据重新生成。底层数据命名为 `results/qN_figXX_内容_data.csv`，或在 README 明确指向已有结果文件及所用字段，避免重复存同一份数据。
- 手工流程图提交可编辑源文件（例如 `.drawio` 或含 Mermaid 源码的 `.md`），不要求虚构 CSV。
- README 中逐张列出：文件链接、中文图题、支持的结论、数据文件及字段、生成脚本和命令。图片修改后同步更新数据和说明。

### 结果文件和表格规范

- 最终明细：`results/qN_results.csv`；评价指标：`results/qN_metrics.csv`。
- 解答中的编号表格：`results/qN_tabXX_内容.csv`，例如 `q2_tab01_method_comparison.csv`。若内容已在结果明细中，允许直接引用其字段，无需另存重复副本。
- CSV 首行是字段名，每行代表一个明确的对象；编号字段保留，不能只靠行号关联作品或专家。
- 在 README 解释字段含义、单位、缺失值处理、排序方向及并列规则。作品或专家编号要说明是否保留前导零。
- 指标注明计算方法、实验条件及越大越好还是越小越好。正文展示可以四舍五入，底层结果保留合理精度。
- 未运行或未验证的指标写“待验证”，不能把示例数值、硬编码展示值或预期效果作为实验结果。

## 三、每问 README 必填模板

复制到对应问题的 `README.md`，将 `N`、示例路径和所有占位内容替换为实际信息。不适用的项填写“不适用：原因”，不能直接删除关键栏目。

````markdown
# 第 N 问：实际标题

## 1. 状态与交接
- 状态：进行中 / 待复核 / 已复核
- 当前维护者：
- 最近更新日期：YYYY-MM-DD
- 已完成：
- 待完成与已知问题：
- 交接说明：下一步任务、涉及文件、尚未 push 的内容（如有）

## 2. 问题目标
本问需要回答什么，输入是什么，最终输出是什么；逐项对应题目要求。

## 3. 数据与预处理
- 输入文件：相对路径、工作表名称、使用字段
- 数据规模与字段含义、单位：
- 缺失值、重复值、异常值处理：
- 与其他问题的依赖及所需结果文件：

## 4. 模型与方法
- 假设与符号定义：
- 核心公式与参数含义：
- 算法步骤、参数取值及选择依据：
- 完整解答：[qN_solution.md](docs/qN_solution.md)

## 5. 环境与运行
- 操作系统、语言版本、依赖版本：
- 命令执行位置：仓库根目录
- 随机种子；若无随机过程，注明不适用：
- 预计耗时：
- 输出路径及会覆盖的文件：

```bash
python -m pip install -r 问题N/requirements.txt
python 问题N/qN_solve.py
python 问题N/qN_plot.py
```

上面的“问题N”必须改成实际中文目录名；若代码入口或运行方式不同，替换为实际验证过的命令。

## 6. 结果与数据字典
- 最终结果文件链接：
- 每个字段的含义、单位、缺失值表示、排序规则：
- 核心结果数值及解释：
- 这些结果具体回答了题目的哪些要求：

## 7. 图表索引
逐张填写以下条目；表格数据也按同样方式登记。
- 图 N-1：中文图题
  - 图片：[查看图](figures/qN_fig01_example.png)
  - 数据：结果文件路径和所用字段
  - 生成方式：脚本路径及可复制的命令
  - 结论：该图说明什么，正文在哪一节引用

## 8. 验证与局限
- 实际执行的验证命令、日期和结果：
- 约束检查、基线对比、误差或稳健性分析：
- 未验证的部分和不能得出的结论：

## 9. 参考资料
资料名称、来源链接或仓库文件路径；明确借鉴的方法及位置。
````

完整解答 `docs/qN_solution.md` 必须对应题目逐项作答；公式中的每个符号要解释，结论要能追溯到结果文件或图表，区分假设、实测结果和推测。若题目要求比较多个方案，必须提供各方案方法及同一条件下的比较结果。

## 四、交付前检查清单

每次声明某问“完成”或请求最终验收时，逐项检查：

- [ ] README 所有必填项已填写，状态和待解决项真实。
- [ ] 完整解答已逐项回答题目要求，符号和公式与代码一致。
- [ ] 代码、依赖、输入路径齐全，没有本人电脑的绝对路径。
- [ ] 按 README 从仓库根目录实际运行成功；记录验证环境及随机种子。
- [ ] 最终结果和评价指标已提交，字段解释完整。
- [ ] 本问至少一张有效图，正文引用的全部图片都已提交。
- [ ] 图号、文件名、图题一致，文字清晰，底层数据和生成方式齐全。
- [ ] 修改模型后已重新生成受影响的结果和图片，或明确标为待更新。
- [ ] 没有把示例数据或待验证指标写成正式结论。
- [ ] PR 写明本次变化、验证结果和剩余问题，另一人可以接手。

不提交 `.venv/`、`__pycache__/`、编辑器缓存、Office 临时文件、账号凭据及无关测试图片。

## 五、首次准备

队友先接受私有仓库邀请，并确保自己的 GitHub 账号具有仓库读写权限。

首次下载仓库执行以下命令；已经克隆过就直接进入现有仓库目录，不要重复克隆。

```bash
git clone https://github.com/timoler/2023-c.git
cd 2023-c
```

在仓库内设置自己的提交身份，把示例姓名和邮箱替换为自己的信息：

```bash
git config user.name "你的姓名"
git config user.email "你的GitHub邮箱"
```

## 六、开始做一问

以下以第三问为例，其他问题替换分支名和目录名即可。命令逐步执行，发生错误或冲突时先处理，不要继续粘贴后续命令。

先运行 `git status`，确认当前修改已经提交，再从最新 `main` 创建分支：

```bash
git status
git switch main
git pull --ff-only origin main
git switch -c q3-model
```

`git switch -c` 只用于首次创建分支。如果本地已有这个分支：

```bash
git switch q3-model
```

如果分支只存在于远程，本地还没有：

```bash
git fetch origin
git switch --track origin/q3-model
```

## 七、日常提交和 push

已建立远程跟踪关系的分支，每次开始工作前同步：

```bash
git switch q3-model
git pull --ff-only
```

首次创建、尚未 push 的本地分支跳过这一步 pull。

在 `问题三/` 中完成一轮修改后，检查并提交：

```bash
git status
git diff -- 问题三/
git add 问题三/
git diff --cached
git commit -m "q3: 完成初版模型与结果"
git push -u origin q3-model
```

如果修改了其他文件，将实际文件路径加入 `git add`。提交前检查暂存内容，避免上传临时文件、账号凭据或无关文件。

首次 push 的 `-u` 用于建立跟踪关系，之后提交完成只需：

```bash
git push
```

提交说明统一采用“问题编号：具体修改”，例如：

```text
q1: 增加随机分配对照实验
q2: 修正专家权重计算
q3: 补充模型敏感性分析
docs: 更新论文第二问模型说明
```

## 八、检查并合并到 main

1. 作者 push 工作分支后，在 GitHub 仓库打开 **Pull requests → New pull request**。
2. `base` 选择 `main`，`compare` 选择自己的工作分支，例如 `q3-model`。
3. 标题说明本次成果；正文写清修改内容、运行方法、验证结果和未解决问题。
4. 另一人检查模型和公式是否一致、代码能否运行、结果与图表是否对应。
5. 需要修正时，作者在原工作分支继续 commit、push，PR 会自动更新。
6. 检查通过后，使用 **Create a merge commit** 合并，便于这四个工作分支持续使用。本项目约定暂不删除仍在使用的分支。

合并完成后，两人各自更新本地 `main`。切换前先提交当前修改：

```bash
git switch main
git pull --ff-only origin main
```

## 九、把队友已合并的成果同步到自己的工作分支

例如第二问已经进入 `main`，正在做第三问的人需要用到它。先提交自己的修改，再执行：

```bash
git switch q3-model
git fetch origin
git merge origin/main
git push
```

注意：在工作分支执行普通 `git pull`，同步的是它跟踪的远程工作分支，不会自动同步 `main`，所以需要上面的 merge。

如果 merge 提示冲突，暂停后续命令：

1. 执行 `git status` 找到冲突文件。
2. 与队友确认应保留的内容，编辑文件并删除冲突标记。
3. 执行 `git add 实际冲突文件路径`。
4. 执行 `git commit` 完成合并，再执行 `git push`。

暂时无法处理时，可执行 `git merge --abort` 取消本次合并。不要通过 `git push --force` 覆盖共享历史。如果 `git pull --ff-only` 报错，也先检查分支历史，不要强制覆盖。

## 十、当前 main / master 怎么处理

目前第一问在 `main`，第二问在 `master` 的 `问题二/` 目录。先由整合负责人做一次迁移，不要直接用一个分支覆盖另一个分支。

下面的方法只把第二问目录引入整合分支，不要求两个旧分支具有共同历史。执行前确认工作区干净，并确认最新 `main` 中尚无需要保留的同名 `问题二/` 目录；如果已有，先比较内容再整合。

```bash
git status
git switch main
git pull --ff-only origin main
git fetch origin
git switch -c integrate-q2
git restore --source=origin/master --staged --worktree -- 问题二/
git diff --cached --stat
git diff --cached
git commit -m "q2: 引入 master 分支的第二问方案"
git push -u origin integrate-q2
```

随后从 `integrate-q2` 向 `main` 发起 PR，检查第二问代码的数据路径、依赖及结果后再合并。这种方式复制第二问文件，原来的提交历史仍保留在 `master` 中。

迁移完成后，第二问负责人从最新 `main` 新建 `q2-standard-score`，后续在新分支继续工作。原 `master` 暂时保留作历史参考，不再继续提交第二问新成果。

## 十一、四个命令各自负责什么

- `git add`：选择本次准备提交的文件修改。
- `git commit`：把修改保存为本地版本，还没有上传。
- `git push`：把本地提交上传到远程分支，不会自动合并进 `main`。
- `git pull`：把远程分支更新同步到当前本地分支；本规范使用 `--ff-only`，历史分叉时会停止并提示。

日常流程：同步自己的分支 → 修改和验证 → commit → push → 发起 PR → 队友检查并合并 → 同步 main。
