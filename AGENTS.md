# 仓库协作指引

适用范围：整个仓库及全部四问。

在分析、修改或交付本仓库内容前，先阅读根目录的 [协作规范.md](协作规范.md)，并遵守其中的命名、目录、文档、图表、结果和 Git 协作要求。

- 不固定谁负责第几问；接手时检查分支状态和对应问题 README 中的交接说明。
- 每问交付必须有 README、完整解答、可运行代码、依赖说明、结果数据和至少一张有效图。
- 新增图片命名为 `qN_figXX_内容.png`，例如 `q2_fig01_expert_distribution.png`；提供可追溯数据及生成方式。
- 每问 README 按协作规范中的必填模板编写；声明完成前逐项核对交付清单。
- 不得把示例值、硬编码展示值或未验证指标写成真实实验结论。
- 修改模型后同步更新受影响的结果、图表和说明；未完成项明确标为待更新或待验证。
- 保留原始题目和数据；旧文件逐步迁移时同步修改路径，不因整理文件破坏既有代码。
- 遵循规范中的工作分支和 PR 流程；未经明确授权不推送、不合并，不覆盖他人修改。

本文件只提供执行入口；具体标准以 `协作规范.md` 为准，避免在多处维护互相矛盾的详细要求。
